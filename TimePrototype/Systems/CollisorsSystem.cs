using System;
using Nez;

namespace TimePrototype.Systems
{
    class CollisorsSystem : EntityProcessingSystem
    {
        public CollisorsSystem() : base(Matcher.empty())
        {
        }



        public override void process(Entity entity)
        {
            throw new NotImplementedException();
        }
    }
}
